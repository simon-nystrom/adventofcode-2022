function join(arr,    a) {
    for (i = 1; i <= length(arr); i++) {
        a = a arr[i];
    }
    return a;
}

function reverse(s,         arr, r) {
    split(s, arr, "")
    for(i = length(arr); i > 0; i--) {
        r = r arr[i]
    }
    return r
}